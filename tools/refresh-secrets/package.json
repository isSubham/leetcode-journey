{
  "name": "refresh-secrets",
  "private": true,
  "description": "Local-only tool to refresh LeetCode session cookies as GitHub Actions secrets. Never runs in CI.",
  "scripts": {
    "start": "node refresh.js"
  },
  "dependencies": {
    "puppeteer": "^22.0.0",
    "@octokit/rest": "^20.0.0",
    "libsodium-wrappers": "^0.7.13",
    "dotenv": "^16.4.0"
  }
}